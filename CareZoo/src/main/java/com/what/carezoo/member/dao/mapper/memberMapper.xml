<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.what.carezoo.member.dao.MemberDao">
	<insert id="insertCustomer" parameterType="customer">
		<selectKey order="BEFORE" keyProperty="c_num" resultType="int">
			select customer_seq.nextval from dual
		</selectKey>
		insert into customer
		values(#{c_num},#{c_name},#{c_email},#{c_pass},#{c_address},#{c_d_address},#{c_sex},#{c_birth},#{c_contact},#{c_e_address})
	</insert>
	<update id="updateCustomer" parameterType="customer">
		update customer 
			set c_contact = #{c_contact},
				c_address = #{c_address},
				c_d_address = #{c_d_address},
				c_pass = #{c_pass}
			where c_num = #{c_num}
	</update>
	<delete id="deleteCustomer" parameterType="int">
		delete from customer where c_num = #{c_num}
	</delete>
	<select id="selectOneByNum" parameterType="int" resultType="customer">
		select * from customer where c_num=#{c_num}
	</select>
	<select id="selectOneByEmail" parameterType="String">
		select * from customer where c_email=#{c_email}
	</select>
	<select id="selectAll" resultType="customer">
		select * from customer
	</select>
</mapper>