<!DOCTYPE mapper     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.PetDao">
	<insert id="insertPet" parameterType="pet">
		insert 
		into pet (p_num, c_num, p_kind, p_name, p_sex, p_birth, p_weight, p_none_sex, p_img)
		values (pet_seq.nextval, #{c_num}, #{p_kind}, #{p_name}, #{p_sex}, #{p_birth}, #{p_weight}, #{p_none_sex}, #{p_img})
	</insert>
	
	<update id="updatePet" parameterType="pet">
		update pet
			set p_weight = #{p_weight},
			p_none_sex = #{p_none_sex},
			p_img = #{p_img}
		where p_num = #{p_num}
	</update>
	
	<delete id="deletePet" parameterType="pet">
		delete from pet where p_num = #{p_num}
	</delete>
	
	<select id="selectPet" parameterType="int" resultType="pet">
		select * 
		from pet
		where p_num = #{p_num}
	</select>
	
	<select id="selectAllPet" resultType="pet">
		select * from pet
	</select>

</mapper>